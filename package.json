{
  "name": "sparxstar-boson-scaffold",
  "version": "1.0.0",
  "description": "SPARXSTAR Boson Scaffold — Node.js dev tooling (Stylelint and markdownlint).",
  "private": true,
  "scripts": {
    "lint:css": "stylelint '**/*.{css,scss}' --ignore-path .stylelintignore",
    "lint:md:workbooks": "markdownlint 'workbooks/**/*.md'",
    "build:workbooks": "bash scripts/export-workbooks.sh",
    "lint": "npm run lint:css && npm run lint:md:workbooks"
  },
  "devDependencies": {
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^36.0.0",
    "markdownlint-cli": "^0.43.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}